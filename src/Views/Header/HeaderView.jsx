import NavigationComponent from '../../Components/Navigation/NavigationComponent';

const HeaderView = () => {
  return (
    <header>
      <NavigationComponent />
    </header>
  );
};

export default HeaderView;
